# Backend test dependencies
# Install with: pip install -r requirements-test.txt

# Core test framework
pytest>=7.4
pytest-asyncio>=0.23
pytest-httpx>=0.28

# HTTP client (for TestClient)
httpx>=0.26

# Load testing (run manually, NOT in CI)
locust>=2.20

# Static analysis
bandit>=1.7
ruff>=0.3

# Required by the app itself (should already be installed)
fastapi>=0.111
uvicorn>=0.29
docker>=7.0
pygdbmi>=0.11
pydantic>=2.0
